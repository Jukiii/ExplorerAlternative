<Window x:Class="ExplorerAlternative.Views.PatchPreviewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Patchの内容確認"
        Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}"
        TextElement.Foreground="{DynamicResource TextPrimaryBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Summary}" FontWeight="Bold" Margin="0,0,0,10" TextWrapping="Wrap" />

        <RichTextBox Grid.Row="1" IsReadOnly="True" IsDocumentEnabled="True"
                     x:Name="PatchTextBox"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                     Background="{DynamicResource WindowBackgroundBrush}"
                     Foreground="{DynamicResource TextPrimaryBrush}"
                     BorderBrush="{DynamicResource BorderBrush2}" BorderThickness="1" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="適用" Width="90" Margin="0,0,8,0" IsDefault="True" Click="ApplyButton_Click" />
            <Button Content="キャンセル" Width="90" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
