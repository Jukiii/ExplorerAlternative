# Windows向け高機能ファイルエクスプローラー 開発仕様書 v3

## AI開発プロンプト

あなたは経験豊富なWindowsデスクトップアプリ開発者です。
以下の仕様を満たす、実用レベルのWindows向け高機能ファイルエクスプローラーを設計・実装してください。

このアプリは **Windows 11 Explorer + 開発者向けターミナル/Git/SVN + macOS FinderのQuick Look + VS Code的な分割/ターミナル操作** を融合したファイルマネージャーを目指します。

---

## 1. 技術スタック

- C#
- .NET 10
- WPF
- Windows 11向けEXE
- MVVM等
- Git CLI
- SVN CLI
- Windows ConPTY
- OpenSSH / 必要に応じてSSH.NET
- Windows Credential Manager等の安全な認証保存

UIとロジックを分離し、拡張可能な構造にすること。

---

## 2. UIデザイン

外観は **Windows 11のエクスプローラーを最優先**。

- Fluent Design
- Windows 11風ツールバー
- Windows 11風アイコン
- Windows 11風の余白
- 角丸
- ホバー
- Light / Dark / System
- 高DPI対応

VS Code風・macOS風の外観にはしない。
ただし、機能面ではVS CodeやFinderの便利な機能を取り入れる。

（実装状況・2026-09更新）アプリアイコンを設定した（`Assets/app.ico`、16〜256pxの
複数解像度を含む）。フォルダをモチーフに、メイン表示モードの階層表示（4.2章）で
使う展開記号「>」を白抜きで配置したデザイン。実行ファイル（タスクバー・タスク
トレイ含む）とウィンドウタイトルバーの両方に反映される。

（実装状況・2026-09更新）ユーザーからの明示的な指示により、配色をClaude.ai風の
暖色系（オレンジ系アクセント・クリーム色の背景）に変更し、あわせて角丸を全体的
に拡大した（ボタン・トグルボタン・ナビ項目・タブ・スクロールバーつまみ等）。本章
本文の「Windows 11のエクスプローラーを最優先」はレイアウト・操作感の指針として
維持しつつ、配色のみユーザー指示を優先する（31章「重要な判断ルール」参照）。
`Themes/LightTheme.xaml`・`Themes/DarkTheme.xaml`の`DynamicResource`ブラシキー
（`WindowBackgroundBrush`等）を差し替えるだけで全画面に反映される構造は変更して
いない。ターミナル・Diff・VCSバナー等、意味を持つ配色（緑=追加、赤=削除等）は
維持している。設定画面（63章）の`TabControl`は、Application.Resourcesでの
カスタムテンプレートがタブ本体の背景に反映されない環境だったため、各タブの中身
（Grid）に直接`WindowBackgroundBrush`を指定する方式で統一感を確保した。

（実装状況・2026-09更新）配色に続けて、ユーザー指示（「余白・間隔の調整」
「ツールバー/アドレスバーの見た目を刷新」「ナビペイン項目をカード風に」）に
従いレイアウトも一部刷新した。ペイン構成・列配置（3章の画面構成図）自体は
変更していない。
- ツールバー：戻る/進む/上へボタンの間隔を拡大し、余白を全体的に広げた。
- アドレスバー・表示モード切替・隠しファイル切替の背景を角丸10のピル形状に統一。
- パンくずドロップダウン（11章）のポップアップとホバー行にも角丸を適用した。
- ナビゲーションペイン（4章・6章）の各項目（お気に入り／タグ／ドライブ／
  ワークスペース／最近使った場所／最近のプロジェクト）を、ペイン背景から
  浮いて見える枠線付きの「カード」形状にし、項目間の余白も広げた
  （`NavListBoxItemStyle`、App.xaml）。

（実装状況・2026-09更新）上記カード化で各項目が縦に大きくなりすぎたため、
ユーザー指示により1項目あたりの高さを縮小した（`NavListBoxItemStyle`の
Padding・Marginを再調整）。カードの枠線・角丸自体は維持している。

---

## 3. 基本レイアウト

```text
┌──────────────────────────────────────────────────────────────┐
│ ← → ↑ ↻   C: > Users > User > Project > src          🔍     │
├────────────────┬──────────────────────┬──────────────────────┤
│ ⭐ お気に入り   │ ▾ 📁 Project         │ ▾ 📁 Backup          │
│ ⭐ Home         │   ▾ 📁 src           │   ▾ 📁 src           │
│ ⭐ Desktop      │     📄 main.cs      │     📄 main.cs       │
│ ⭐ Downloads    │     📄 test.cs      │     📄 test.cs       │
│ ⭐ Documents    │   📄 README.md      │   📄 README.md       │
│                │                      │                      │
│ 🏷 タグ         │      ACTIVE          │                      │
│ ⭐ 重要         │                      │                      │
│ 🔵 作業中       │                      │                      │
│ 🟢 完了         │                      │                      │
├────────────────┴──────────────────────┴──────────────────────┤
│ PowerShell 1 │ PowerShell 2 │ Git │ +          [SYNC ●]     │
├───────────────────────────────────────────────────────────────┤
│ PS C:\Project>                                               │
└───────────────────────────────────────────────────────────────┘
```

右側に独立した常設ファイル一覧ペインは作らない。
ファイル表示はメイン領域内で行う。

（実装状況）Git/SVN情報（21章）は、上記の「ファイル一覧ペイン」とは別に、
表示/非表示を切り替え可能な右側の状態表示ペインとして実装している
（既定は表示、ハンバーガーメニューまたはペイン内ボタンで切替）。

（実装状況・2026-09更新）左のナビゲーションペインと右のGit/SVN情報ペインは、
PowerShellターミナル（17章）の表示/非表示に関わらず、常にウィンドウの下端まで
伸びる。ターミナルは中央のメインペイン（ファイル一覧）列の下側にのみ配置され、
その列の幅に自動的に一致する（左右のペインの下には表示されない）。

---

## 4. 左ペイン

お気に入り・ナビゲーション用。

標準項目：

- 各ドライブ
- 最近使った場所
- お気に入り（Home/Desktop/Downloads/Documents/Pictures/Music/Videosを初期値として含む）
- タグ
- ワークスペース

ユーザーは任意のフォルダを追加可能。

- 追加
- 削除
- 並び替え
- ドラッグ＆ドロップ
- 右クリックメニュー

左ペインは表示/非表示、幅変更に対応し状態を保存する。

（実装状況・2026-09更新）当初はクイックアクセス（Home/Desktop等の固定一覧）と
お気に入りを別セクションとして表示していたが、ユーザー指示により統合。初回起動時
（お気に入りが空の場合）のみHome/Desktop/Downloads/Documents/Pictures/Music/Videos
をお気に入りの初期値として登録し、以降は他のお気に入りと同様に追加・削除・
名前変更・並び替え（▲▼ボタンおよびお気に入り欄内でのドラッグ&ドロップ）ができる
単一の一覧として扱う。ピン留めファイル（51章）はユーザー指示により削除した。

（実装状況・2026-09更新）GridSplitterで左ペイン・右のGit/SVN情報ペインの幅を
変更しても表示内容の幅が追従しない不具合を修正した（Border側に固定Widthを
指定していたため、列（枠）は広がっても中身の表示幅が変わらなかった。Borderの
指定をMinWidthに変更し、列幅にHorizontalAlignment=Stretchで追従するようにした。
折りたたみ時に列が縮む挙動は維持している）。

（実装状況・2026-09更新）上記MinWidthは60pxまで縮小し、それより狭く
GridSplitterで縮めた場合は水平スクロールバー（ScrollViewer）で中身を
確認できるようにした。また、GridSplitterはドラッグ後もキーボードフォーカスを
持ち続け、直後に矢印キーを押すと（ファイル一覧の移動のつもりでも）列幅が
変わってしまう問題があったため、GridSplitterをFocusable="False"にして
キーボードフォーカスを受け取らないようにした（マウスドラッグでの
リサイズ自体には影響しない）。

（実装状況・2026-09更新）ユーザー指示により、左ペインの項目の表示順を
お気に入り→タグ→ドライブ→ワークスペース→最近使った場所→最近のプロジェクト
に変更した。

（実装状況・2026-09更新）トラックパッドの横スワイプ（`WM_MOUSEHWHEEL`）による
水平スクロールに対応した。WPFのScrollViewerは既定でこれをハンドリングしない
ため、ウィンドウメッセージを直接フックし、カーソル直下のScrollViewerへ手動で
反映する方式にした（左右ペイン・ファイル一覧・Git/SVN情報ペインなど、
ScrollViewerを持つ箇所すべてに適用される）。

（実装状況・2026-09更新）上記の実装には、カーソル直下から祖先方向へ最初に
見つかったScrollViewerをそのまま使ってしまうという不具合があった。ナビペイン
内の各ListBoxが持つ内部ScrollViewer（既定では横スクロール無効）に当たってしまい、
ペイン全体を横スクロールしたいケースで実際には何も起きていなかった。横方向に
実際にスクロール可能な最も内側のScrollViewerを優先して探すように修正した
（`FindScrollViewerAt`、MainWindow.xaml.cs）。

（実装状況・2026-09更新）「最近使った場所」等、MaxHeightで内部スクロールが
必要な一覧で、ホイール操作しても一覧内をスクロールできない不具合を修正した。
原因は、ナビペイン内のListBoxのホイールイベントを常に外側のペイン全体の
ScrollViewerへ転送していたため（4章の「內側のListBoxが自身でスクロールを
消費してしまう問題」への対処が、逆に内部スクロールが必要な一覧の妨げに
なっていた）。内側のScrollViewerがその方向へまだスクロールできる間は
内側に処理させ、端に達している場合のみ外側へ転送するように修正した
（`NavListBox_PreviewMouseWheel`、MainWindow.xaml.cs）。

（実装状況・2026-09更新）トラックパッドの横スワイプが、MainWindow以外の
ウィンドウ（Log/フォルダ比較/SFTPブラウザ・Diff等）では一切効かない不具合を
修正した。`WM_MOUSEHWHEEL`のフックがMainWindowのHWNDにしか登録されておらず、
別ウィンドウ（別HWND）にはメッセージが届かないため何も起きていなかった。
フック処理を`TrackpadScrollSupport`（アプリ直下）へ切り出し、`Window`型の
`Loaded`イベントをクラスハンドラで一括購読することで、以降に開かれる
全てのWindow（MainWindow含む）へ自動的に適用されるようにした
（タイトルバーの明暗をDWM経由で全Windowへ適用する仕組みと同じ方式）。

（実装状況・2026-09更新）ユーザー指示により、横スクロールが存在する箇所全てで、
トラックパッドの横スワイプに加えて、通常ホイール＋Shift（Windows/Office等で
一般的な「Shiftを押しながらホイール＝横スクロール」の慣習）にも対応した。
`TrackpadScrollSupport`が`WM_MOUSEHWHEEL`（トラックパッド）と`WM_MOUSEWHEEL`
（Shift押下時のみ横スクロールとして扱う）の両方をフックする。個別の
Window/ScrollViewerで独自に実装する必要はなく、アプリ内のどのWindow・
ScrollViewer（ナビ/VCSペイン、Log、フォルダ比較、Diff、Quick Look画像
プレビュー等）でも自動的に両方の操作に対応する。新しく追加するWindowでも
`TrackpadScrollSupport.RegisterForAllWindows()`（App.xaml.cs起動時に1回登録
済み）により自動的に有効になるため、追加対応は不要。

---

## 5. タグ

左ペインにタグ一覧を表示。

```text
🏷 タグ
⭐ 重要
🔵 作業中
🟢 完了
📦 アーカイブ
```

- ファイル/フォルダに複数タグ
- タグ追加・削除・名前変更
- アイコンを選択可能
- 必要なら色も設定可能
- タグクリックで該当ファイル/フォルダ一覧を表示
- タグ情報はアプリ側で管理しファイル本体を変更しない

---

## 6. メイン表示モード

**通常の単純リスト表示は用意しない。**

切り替え可能なモード：

### 6.1 階層表示（主要モード）

```text
▾ 📁 Project
  ▾ 📁 src
    ▸ 📁 components
      📄 Button.cs
      📄 Header.cs
    📄 main.cs
  ▸ 📁 assets
  ▸ 📁 tests
  📄 README.md
```

- ファイルとフォルダを同じツリーに表示
- `▸` / `▾` を使用
- `+` / `-` は使用しない
- Lazy Loading
- UI Virtualization
- 非同期列挙
- フォルダに入らなくても下位階層を展開できる

（実装状況・2026-09更新）フォルダの展開・折りたたみ時は、表示リスト全体を
作り直すのではなく、開閉に伴い増減した項目だけを挿入・削除するようにした
（不具合修正：以前は展開・折りたたみのたびに表示リストをClear→再構築して
いたため、選択位置とスクロール位置が先頭に戻ってしまっていた）。

### 6.2 詳細表示

Windows 11 Explorer風。

```text
名前              更新日時             種類          サイズ
────────────────────────────────────────────────────
📁 Project         2026/09/01 22:10     フォルダー
📄 main.cs         2026/09/01 21:32     C#ファイル      12 KB
📄 README.md       2026/08/31 18:20     Markdown         4 KB
```

列候補：

- 名前
- 更新日時
- 作成日時
- 種類
- サイズ
- Git状態
- SVN状態
- タグ

列の表示/非表示、幅、順序を保存する。

（実装状況・2026-09更新）名前・サイズ・更新日時・種類・タグの列を実装。当初は
「種類」の代わりに「Git状態」の列を表示していたが、Git状態はファイル一覧の
バッジ表示（21章）で既に確認できるため、ユーザー指示により「種類」列に差し替えた
（拡張子ベースの簡易表示、例：「CSV ファイル」。フォルダは「ファイル フォルダー」）。
列ヘッダーの右クリックには何も割り当てていない（列の表示/非表示切替等は将来検討）。

（実装状況・2026-09更新）名前・サイズ・更新日時・種類の各列ヘッダーをクリックすると、
その列を基準に並び替えるようにした。同じ列を再度クリックすると昇順/降順が反転する。
フォルダは並び替え条件に関わらず常に先頭にまとまる。現在の並び替え列のヘッダーには
▲（昇順）/▼（降順）を表示する。タグ列はクリックしても並び替えは行わない。

---

## 7. 階層キーボード操作

- `↑ / ↓` 項目移動
- `→` フォルダ展開
- `←` フォルダ折りたたみ（展開中でない項目の場合は親フォルダを選択）
- `Alt + ↑` 親階層へ
- `Enter` 開く
- `F2` 名前変更
- `Delete` 削除
- `Space` Quick Look

（実装状況・2026-09更新）`←`は、選択中の項目が展開中のフォルダであればそれを
折りたたむ。それ以外（ファイル、または折りたたみ済みのフォルダを選択中）の
場合は、Windows標準のツリー操作に合わせて親フォルダを選択する（もう一度
`←`を押すと、選択された親フォルダが展開中であれば折りたたまれる）。

---

## 8. メイン領域の分割ビュー

**メイン表示領域だけ**を分割する。
左ペイン、ツールバー、ターミナル等は分割しない。

```text
┌────────────────────┬────────────────────┐
│ C:\Project         │ D:\Backup          │
│                    │                    │
│ ▾ 📁 src           │ ▾ 📁 src           │
│   📄 main.cs       │   📄 main.cs       │
│   📄 test.cs       │   📄 test.cs       │
└────────────────────┴────────────────────┘
```

機能：

- 縦分割
- 横分割
- 分割解除
- 左右/上下入れ替え
- ペインサイズ変更
- ペイン間フォーカス移動
- 複数ペイン
- ペインごとに独立した現在パス
- ペインごとに独立した表示モード

推奨：

- `Ctrl + \` 縦分割
- `Ctrl + Shift + \` 分割解除

ショートカットは設定から変更可能。

（実装状況・2026-09更新）`Ctrl + \`（右に分割）・`Ctrl + Shift + \`（ペインを
閉じる＝分割解除）のショートカットを実装した（メニュー・コマンドパレットからは
既に実行可能だったが、キーボードショートカットが未実装だった）。ペインサイズ
変更（GridSplitter等によるドラッグリサイズ）・左右/上下入れ替えは今後の課題。

---

## 9. アクティブペイン

分割ビューでは必ずアクティブペインを持つ。

ファイル・フォルダ・余白をクリックしたペインをアクティブにする。

### 視覚的表示

アクティブペインは **背景を明確に強調**。

単なる細い枠線だけにせず、背景色・透明度・選択状態等を使い、現在操作対象のペインが一目で分かるようにする。

Light / Dark両方で十分な視認性を確保。

アクティブペインに以下を適用：

- アドレスバー
- 検索
- Git / SVN
- ファイル操作
- Quick Look
- キーボード操作
- Terminal Sync
- コピー/移動
- 新規作成
- 表示モード

（実装状況・2026-09更新）当初はアクティブペインを青枠＋青背景（強度は設定の
「アクティブペイン背景強調度」で調整）で強調していたが、ユーザー指示により
方式を変更。現在はアクティブ側を着色するのではなく、非アクティブ側のペインに
黒の半透明オーバーレイ（同じ強調度設定を流用）をかけて少し薄暗くすることで、
相対的にアクティブペインを目立たせている。

---

## 10. タブ

- `Ctrl + T` 新規タブ
- `Ctrl + W` タブを閉じる
- `Ctrl + Tab` タブ切替
- タブ固定
- スマートタブ
- 同一フォルダの重複タブを抑制可能

タブごとに可能な限り以下を保持：

- 現在パス
- 履歴
- 表示モード
- 展開状態
- ペイン構成
- ターミナル状態

### Ctrlドラッグによるタブ複製

タブを `Ctrl` を押しながらドラッグすると、そのタブを複製する。

複製時には可能な限り以下を引き継ぐ：

- 現在パス
- 履歴
- 表示モード
- 展開状態
- ペイン構成
- ターミナル状態
- Quick Look状態

将来的なタブの別ウィンドウ分離にも対応できる設計にする。

---

## 11. アドレスバー

Windows 11 Explorer風パンくず。

```text
C: > Users > User > Project > src
```

各階層をクリックして移動。

各階層にドロップダウンを用意する。

例：

```text
C: > aa > 2026 > 05
       ▼
```

`aa` のドロップダウン：

```text
C:\
├─ aa
├─ ab
├─ ac
└─ Documents
```

現在 `C:\aa\2026\05` で `ab` を選択した場合：

- 左クリック → `C:\ab`
- 右クリック → `C:\ab\2026\05`

つまりパスの一部だけ差し替え、下層を維持する。

下層が存在しない場合は安全に処理する。

### URL/パス直接入力

- `Ctrl + L`
- `F6`

でアドレスバーへフォーカス。

（実装状況）パンくずの各階層クリック（移動）・ドロップダウン（兄弟フォルダ一覧）・
ドロップダウン項目の左クリック（パス全体を置換）／右クリック（下層を維持したまま
部分置換、下層が存在しない場合は安全にフォールバック）まで実装済み。加えて、
パンくず部分の空白（各セグメント・ドロップダウン矢印以外）をクリックしてもアドレス
編集モードに切り替えられる（Windows 11 Explorer同様の操作感）。`Ctrl+L`/`F6`での
フォーカスも実装済み。ドロップダウンを開いた状態でそれ以外の場所をクリックすると
自動的に閉じる。

（実装状況・2026-09更新）ドロップダウン項目はButtonではなく素のBorderへ直接
マウスイベントを割り当てる方式に変更し、文字部分だけでなく行（枠）全体を
クリック対象にした（以前はContentPresenterの内容幅にしかButtonが広がらず、
行の余白部分をクリックしても反応しない不具合があった）。また、Popupを
`StaysOpen="False"`のままにすると、ポップアップの自動クローズ処理とボタンの
クリック確定処理が競合し、クリックそのものが無視される（左右クリックとも
ナビゲーションが発生しない）WPFの既知の問題があったため、`StaysOpen="True"`とし、
選択時に明示的に閉じる方式・および独自の「他をクリックしたら閉じる」処理に変更した。

（実装状況・2026-09更新）ユーザー指示により、セグメント区切りをWindows 11風の
「>」に変更した（旧「⌄」ボタン＋「/」区切りを廃止し、「>」自体がドロップダウン
開閉ボタンを兼ねる）。また、アドレス編集モード中に編集欄以外の場所をクリックすると、
編集内容を破棄して元のパンくず表示に戻すようにした（クリックだけではフォーカスが
移動しない空白領域も多いため、`TextBox.LostFocus`に加えてウィンドウ全体の
`PreviewMouseDown`でも判定している）。

（実装状況・2026-09更新）ユーザー指示により、「>」の位置をセグメント名の後ろから
前（対象の左側）へ変更した（例：「> C: > Users」のように、各セグメントの手前に
表示される）。また、パスを編集モードへ切り替える手段としてクリック・`Ctrl+L`・
`F6`が既にあるため、アドレスバー右端の鉛筆アイコン（✎）ボタンは廃止した。

---

## 12. 検索

`Ctrl + F`。

対象：

- ファイル名
- フォルダ名
- 拡張子

現在フォルダ以下を検索。

将来的なファイル内容検索に拡張可能な設計。

（実装状況）実装済み。`Ctrl + F`でダイアログを開き、現在フォルダ以下を
ファイル名（拡張子含む）の部分一致でバックグラウンド検索する。キャンセル可能。
検索処理は`IFolderScanService`に切り出しており、将来のファイル内容検索
拡張を妨げない。結果をダブルクリックするとそのフォルダへ移動する。

---

## 13. Quick Look

ファイル選択後 `Space`。

対応：

- PNG
- JPG / JPEG
- GIF
- WebP
- BMP
- TXT
- Markdown / `.md`
- JSON
- CSV
- ソースコード
- PDF
- Officeファイル

### Markdown

Markdownをレンダリング表示。

対応候補：

- 見出し
- 太字
- 斜体
- リンク
- 箇条書き
- 番号付きリスト
- コードブロック
- テーブル
- 引用

「レンダリング表示 / ソース表示」の切り替えを用意する。

### テキスト拡張子

設定から自由に追加・削除。

例：

`.txt`, `.log`, `.ini`, `.conf`, `.cfg`, `.env`, `.properties`

登録した未知の拡張子もテキストとしてプレビュー。

### Office

可能なら閲覧用プレビュー：

- `.xlsx` シート/セル簡易表示
- `.pptx` スライド表示
- `.docx` 文書表示

完全なOffice編集機能は不要。

### 操作

- `Space` 開閉
- `Esc` 閉じる
- `← / →` 前後
- 画像ズーム/パン
- PDFページ移動

---

## 14. フォルダQuick Look

フォルダを選択して `Space`。

表示：

- フォルダ名
- パス
- 項目数
- ファイル数
- フォルダ数
- サイズ
- 更新日時
- Git / SVN状態
- タグ
- 最近変更されたファイル

サイズ計算は非同期・キャンセル可能。

---

## 15. Quick Look固定

プレビューを固定可能。

固定中は別項目を選択してもプレビュー内容を保持。

---

## 16. コードシンボル表示

Quick Lookのソースコードプレビューにシンボル一覧を追加。

対応候補：

- C#
- C/C++
- Python
- Java
- JavaScript
- TypeScript
- Go
- HTML
- CSS

例：

```text
📑 Symbols

class MainWindow
├─ Initialize()
├─ LoadFile()
└─ SaveFile()
```

シンボルクリックで該当位置へジャンプ。

完全なIDEではなく、Quick Lookでのコード確認を高速化することを目的とする。

---

## 17. PowerShellターミナル

画面下部に統合ターミナル。

Windows ConPTYを利用。

- 複数タブ
- 追加/削除
- コピー/ペースト
- ANSIカラー
- スクロール
- Ctrl+C等
- リサイズ
- PowerShellプロファイル

`Ctrl + @` で表示/非表示。

高さはドラッグ変更可能。

（実装状況）複数タブ・タブの追加/削除・スクロール・PowerShellプロファイル読込
（設定でON/OFF）・`Ctrl + @`表示切替（表示直後およびターミナル部分クリック時に
入力欄へ自動フォーカス）まで実装済み。標準入出力のリダイレクト方式のため、
ConPTY・ANSIカラー表示・Ctrl+C等のシグナル伝達・高さのドラッグリサイズは
未実装（現状は固定高さ）。コピー/ペーストは通常のテキストボックスとして
Ctrl+C/Ctrl+Vで可能。

（2026-09調査）ConPTY化を試みたが、`CreatePseudoConsole`/`CreateProcess`等の
Win32 API呼び出しはすべて成功を返すにもかかわらず、疑似コンソール経由で
起動した子プロセス（`cmd.exe`・`powershell.exe`のいずれでも再現）が起動直後
（100ms以内）に`STATUS_DLL_INIT_FAILED`（0xC0000142）で異常終了する現象が、
開発環境・ユーザーの実機の両方で再現した。Microsoft公式サンプルと同一の実装
（構造体レイアウト・`UpdateProcThreadAttribute`の`lpValue`がHPCONへの
ポインタである点を含む）で検証済みのため、実装バグの可能性は低いと判断し、
標準入出力リダイレクト方式へ戻した。

調査で切り分けた事実：
- ユーザーの実機ではWindows Terminal（ConPTYを内部利用）・VS Codeの統合
  ターミナル（node-pty経由でConPTYを内部利用）はいずれも正常に動作しており、
  OS自体がConPTYに完全非対応というわけではない。
- `STARTUPINFOEX`の拡張属性リスト機構自体（属性0件で`EXTENDED_STARTUPINFO_PRESENT`
  のみ指定）は正常に動作し、失敗するのは疑似コンソール属性
  （`PROC_THREAD_ATTRIBUTE_PSEUDOCONSOLE`）を設定した場合のみ。
- `CREATE_NO_WINDOW`・`bInheritHandles`（true/false）・OS互換性マニフェストの
  有無・.NETのCoreCLRホストか`Add-Type`経由のPowerShellホストか、いずれの
  組み合わせでも同一の失敗が再現。
- Windows Terminalアプリの再インストール、`sfc /scannow`（整合性違反なし）、
  `DISM /RestoreHealth`（正常完了）、再起動、いずれでも解消せず。
- 実機にはConPTYの同梱ホスト`C:\Windows\System32\OpenConsole.exe`が存在せず、
  Windows TerminalのStoreパッケージ内のコピーに依存している（一般的な構成
  ではない可能性がある）。
- Process Monitorでの実機キャプチャにより、失敗の瞬間に`conhost.exe`が
  名前付きパイプの読み取りで`PIPE BROKEN`を検出していること、また
  `CreateProcess`を呼び出した側のプロセス自身が`conhost.exe`を直接
  開こうとする形跡（本来ConPTY使用時は発生しないはずの挙動）を確認した。
  `HKCU\Console\%%Startup`の`DelegationConsole`/`DelegationTerminal`を
  明示的に従来のWindowsコンソール（`{B23D10C0-E52E-411E-9D5B-C09FDF709C7D}`）
  に固定しても解消しなかった。

以上より、このマシン固有のOS内部の状態（コンポーネントのバージョン不整合等、
未特定）に起因する可能性が高いと判断し、これ以上の深掘り（WinDbgでの
カーネルレベルのデバッグが必要）は行わず、標準入出力リダイレクト方式を
維持する方針とした。再挑戦する場合は、上記の切り分け結果を出発点にすること。

---

## 18. Terminal Sync

ターミナル左上にトグル。

```text
PowerShell 1                     [ SYNC ● ]  +
```

ON：

Explorerとターミナルのカレントディレクトリを同期。

OFF：

完全独立。

Sync状態はターミナルごとに保持。

（実装状況）実装済み。

（実装状況・2026-09更新）メインペイン内でファイルをクリックする（ペインを
切り替えたわけではない）たびに、同期先へ`cd`（`Set-Location`）コマンドが
繰り返し送られてしまい、ユーザーが何も操作していないように見えるのに
ターミナルへ入力が表示され続ける不具合を修正した。原因は、アクティブペイン
切替時の同期処理（`MainWindowViewModel.SetActivePane`）が、対象のペインが
既にアクティブだった場合でも無条件に同期コマンドを送っていたため。ペインが
実際に切り替わった場合にのみ同期するように修正した。

（実装状況・2026-09更新）ターミナル出力欄（`TerminalOutputTextBox`）の内容を
ドラッグ選択・コピーできない不具合を修正した。原因は、ターミナル部分（出力欄
を含む）をクリックした際に無条件で入力欄へフォーカスを奪う処理があり、出力欄
でのドラッグ選択中にも毎回フォーカスが入力欄へ移動してしまい、選択が
成立しなかったこと。出力欄自体のクリックはこの対象から除外し、通常の
テキストボックスとして選択・コピーできるようにした（入力欄への自動
フォーカスは、出力欄以外のターミナル部分をクリックした場合のみ行う）。

---

## 19. ターミナル連携

- ファイルをTerminalへドラッグ → パス入力
- フォルダをTerminalへドラッグ → パス入力
- TerminalからExplorerへのドラッグ
- 「ここでPowerShellを開く」
- 「ここでターミナルを開く」
- 「SSHターミナルを開く」

（実装状況）ファイル/フォルダのTerminalへのドラッグ→パス入力、「ここで
PowerShellを開く」（外部ウィンドウ）、「ここでターミナルを開く」（統合
ターミナル）は右クリックメニューから実装済み。「SSHターミナルを開く」は
ハンバーガーメニューの「SSH接続...」から実行可能（フォルダ右クリックからの
専用メニューは未実装）。TerminalからExplorerへのドラッグは未実装。

---

## 20. Git / SVN自動判定

現在パスから親方向へ探索。

### Git

`.git` が存在するディレクトリ、またはその配下ならGit UIを表示。

### SVN

`.svn` が存在する場合SVN UIを表示。

### どちらもない

Git/SVN関連UIを表示しない。

両方存在する場合は両方を認識する。

---

## 21. Git

- Status
- Stage
- Unstage
- Commit
- Push
- Pull
- Fetch
- Branch
- Checkout
- Merge
- Rebase
- Stash
- Stash Pop
- Clone
- Initialize
- Log
- Show Commit
- Discard Changes

Explorer上：

- M Modified
- A Added
- D Deleted
- U Untracked
- R Renamed

### Gitブランチ表示

現在のブランチをUIに表示。

```text
📁 Project
🌿 main
```

GUIから：

- ブランチ一覧
- Checkout
- 新規ブランチ作成
- リモートブランチ表示

未コミット変更がある場合は安全確認。

（実装状況）Status/Stage/Commit/Push/Pull/Fetch/Branch切替/新規ブランチ作成/Merge/
Rebase/Stash/Stash Pop/Clone/Initialize/Discard Changes/Logまで実装済み。表示は
上部バナーではなく、表示/非表示を切り替え可能な右側ペインで行う（ハンバーガー
メニューまたはペイン内の折りたたみボタンから切替）。Explorer上のM/A/D/U/R
バッジは階層表示・詳細表示の両方に表示される。Logは右側ペインの「状態」の下に
「コミット履歴」として直近20件を新しい順に表示する（Git: `git log`、
SVN: `svn log`。VCSのルートフォルダが変わった時のみ再取得し、同じリポジトリ内の
移動では再実行しない）。

（実装状況・2026-09更新）ブランチ切替・Merge・Rebaseで選択するブランチ一覧
（`GetBranches`）は、以前から`git branch -a`によりリモートブランチも含めて
取得していたが、ローカル/リモートが入り混じって分かりにくかった。参照名
（`refs/heads/` / `refs/remotes/`）で判定してローカルブランチを先に、
リモートブランチを後にまとめ、それぞれ名前順に並べるよう改善した
（`origin/HEAD`のようなシンボリック参照は選択肢から除外）。

（実装状況・2026-09更新）Show Commitを実装した。右クリックメニュー「Git/SVN操作」
→「Log...」から、直近200件のコミット履歴と選択したコミットの変更内容
（`git show <hash>` / `svn diff -c <rev>`）を1つのウィンドウ（左：一覧、右：
diffテキスト）で確認できる。右側ペインの簡易コミット履歴（直近20件）の各項目を
クリックしても、そのコミットを選択した状態で同じウィンドウが開く。

（実装状況・2026-09更新）Show Commitのdiff表示で、日本語ファイル名が
`"a/docs/\344\273\225\346\247\230.md"`のような8進数エスケープ表記になり
文字化けして見える不具合を修正した。原因はGitの既定設定`core.quotepath=true`
（非ASCII文字を含むパスをエスケープする）で、UTF-8デコード自体の問題では
なかった。`git show`・`git status --porcelain`の呼び出しに
`-c core.quotepath=false`を付与し、パスをそのままのUTF-8文字で受け取るように
修正した（ステータスバッジの対象パス照合にも影響する不具合だったため、
あわせて修正）。

Log機能の実装中に発見・修正した不具合：git/svnの子プロセスの標準出力を読み取る
際、文字エンコーディングを明示していなかったため、.NETがシステムのANSI/OEM
コードページ（日本語Windowsでは既定でCP932）で解釈してしまい、日本語の
コミットメッセージ・ファイル名が文字化けしていた。`VersionControlService`・
`PatchService`のプロセス起動箇所すべてでUTF-8を明示するよう修正し、Status
表示・Diff・Patchも含めて解消した。

（実装状況・2026-09更新）フォルダ移動のたびに、Git/SVNのステータス取得
（`git status`/`svn status`のプロセス起動、最大5秒待ち）・コミットログ取得・
プロジェクト検出（54章、複数階層にまたがるファイル列挙）がUIスレッド上で
同期実行され、起動時・フォルダ移動のたびにアプリがもたつく不具合を修正した。
ファイル一覧の表示自体は従来通り即座に行い、これらの重い処理はバックグラウンド
スレッドへ逃がして完了後にUIへ反映するようにした（世代番号で管理し、反映前に
別フォルダへ移動していた場合は結果を破棄する）。Git状態バッジ・コミット履歴・
プロジェクトルート判定は、フォルダを開いた直後は空で、少し遅れて表示される。

---

## 22. SVN

可能な範囲でGitと同等のUI思想。

- Status
- Add
- Revert
- Commit
- Update
- Log
- Diff
- Copy
- Move

（実装状況・2026-09更新）「.svnを読み込めない」という報告を調査した。
`.svn`フォルダ自体の検出ロジック（`VersionControlService.Detect`、20章）は
`.svn`という名前のファイル/フォルダが祖先方向に存在するかを見るだけで、
`svn.exe`の有無に依存しないため正しく動作することを、ダミーの`.svn`フォルダ
で確認済み。実際に発生していたのは、Subversionのコマンドラインクライアント
（`svn.exe`）がインストールされていない/PATHに無い環境で、状態取得が
「SVN情報の取得に失敗しました（svn.exeが見つかりません）」というメッセージに
なる状況だった可能性が高い（TortoiseSVNはデフォルトではコマンドライン
ツールをインストールしないため、SVN管理下のフォルダでもこのメッセージに
なりやすい）。アプリのバグではなく環境要因の可能性が高いため、ユーザーへ
確認中。

---

## 23. Diff

Git Diffと同じUI/表示形式を基本とする。

Git管理外でも2ファイル比較可能。

- 左右比較
- 行単位Diff
- 差分強調
- 次/前の差分
- Diff保存

（実装状況）実装済み（LCSベースの行単位比較）。Git/SVN管理下では選択ファイルの
コミット済み内容との比較（Show Diff）、管理外でも任意の2ファイル比較（25章）に
利用する。

---

## 24. Patch

- Patch作成
- Patch適用
- Patch内容確認
- PatchからDiff表示

右クリック：

```text
Git
├─ Show Diff
├─ Create Patch
└─ Apply Patch
```

適用前に日本語で対象と変更内容を確認できる。

---

## 25. ファイル比較

任意の2ファイルを選択して比較。

Git管理外でもGit Diff風UI。

右クリック：

`比較`

### 比較対象として保持

右クリック：

`比較対象として保持`

別ファイルを選択後：

`比較対象と比較`

（実装状況）「比較対象として保持」「比較対象と比較」は実装済み（23章のDiff
ビューアを共用）。

（実装状況・2026-09更新）ファイルを2つ選択した状態で右クリックすると表示される
「選択した2ファイルを比較」から、保持→比較の2手順を省いて直接比較できるように
なった（3つ以上選択時、またはフォルダを含む選択時は無効）。

で比較開始。

---

## 26. ファイル操作キュー

大量コピー/移動を管理。

- 一時停止
- 再開
- キャンセル
- 上書き
- スキップ
- リネーム
- 同名ファイル競合
- 複数操作キュー

UIをブロックしない。

---

## 27. 一括リネーム

右クリックメニューに **「一括リネーム」**。

- プレフィックス
- サフィックス
- 連番
- 文字列置換
- 拡張子変更
- 正規表現
- 大文字/小文字変換
- 全角/半角変換
- Unicode正規化
- 日付追加
- 作成日時追加
- 更新日時追加
- プレビュー

（実装状況・2026-09更新）「パターン」モード（連番`{n}`・`{n:000}`でゼロ埋め・
元の名前`{name}`・拡張子`{ext}`・日付`{date}`/`{date:書式}`）と、「検索と置換」
モード（文字列置換・正規表現・大文字小文字区別）の2モードを実装した。
どちらのモードにも追加で、大文字/小文字変換・全角/半角変換・Unicode正規化
（NFKC）を適用できる。プレビューは常にリアルタイムに再計算され、実際の
リネームもプレビューと同じ計算結果（`BulkRenamePreviewItem`）をそのまま使う
ため、プレビューと結果が食い違うことはない。作成日時・更新日時の追加は
未実装（今後の拡張候補、`{date}`は現在時刻のみ対応）。

---

## 28. パスコピー

右クリック：

- フルパスをコピー
- ファイル名をコピー
- フォルダパスをコピー
- 相対パスをコピー
- URIをコピー

---

## 29. Undo

Explorer内の操作を履歴化。

対象：

- Delete
- Rename
- Move
- Copy

`Ctrl + Z` で可能な範囲で復元。

---

## 30. GUI Undo履歴

GUIから操作履歴を確認。

```text
操作履歴

☑ main.cs を移動
☑ test.cs を名前変更
☑ old.log を削除
```

戻したい地点を選択してUndo可能。

復元できない操作は明示。

---

## 31. ファイル操作履歴

本アプリが実行した操作を記録。

- 時刻
- 操作
- 対象
- 元の場所
- 移動先
- 成否

履歴の検索・削除に対応。

---

## 32. ファイル操作プレビュー

操作前に日本語で確認。

```text
┌──────────────────────────────────────┐
│ 操作内容を確認                       │
├──────────────────────────────────────┤
│ 23個の項目を移動します。             │
│                                      │
│ 移動元：C:\Project                   │
│ 移動先：D:\Backup                    │
│                                      │
│ [キャンセル]               [実行]    │
└──────────────────────────────────────┘
```

設定からON/OFF。

---

## 33. ドラッグ＆ドロップ

- 通常ドラッグ → 移動
- `Ctrl` + ドラッグ → コピー
- `Alt` + ドラッグ → ショートカット
- フォルダ上で停止 → 自動展開
- ドロップ先をハイライト
- ターミナルへのドラッグ対応
- ペイン間ドラッグ対応

（実装状況）通常ドラッグは同一ドライブなら移動・異なるドライブならコピー
（Windows標準準拠）。`Shift`+ドラッグで移動を強制、`Ctrl`+ドラッグでコピーを
強制、`Alt`+ドラッグでショートカット（.lnk。WScript.Shell COM経由、追加の
NuGet依存なし）を作成できる。ターミナルへのドラッグ（パス入力）・ペイン間
ドラッグは実装済み。フォルダ上で停止した際の自動展開・ドロップ先のハイライトは
未実装（今後の拡張候補）。

（実装状況・2026-09更新）ユーザー指示により、`Ctrl`+ドラッグの詳細動作を以下の
通りとした。
- 同じフォルダ内へドロップ：複製として扱う（Ctrl+Dの複製機能と同じ「名前 (2)」
  方式の採番）。
- 異なるフォルダへドロップし、移動先に同名の項目が既に存在する場合：
  「名前を変更してコピー（`*_copy`方式、重複時は`_copy2`のように採番）」／
  「上書きする」／「キャンセル」をダイアログで確認する。

---

## 34. アプリで開く

右クリック：

`アプリで開く`

- インストール済みアプリ候補
- 任意のEXE
- 今回だけ指定
- 常にこのアプリ
- 拡張子関連付け管理

設定画面から変更可能。

（実装状況）ファイルの右クリックメニュー「アプリで開く」から「既定のアプリで開く」
「アプリを選択...」（任意のEXEを選択して今回だけ開く）まで実装済み。「常にこの
アプリ」「拡張子関連付け管理」（既定のアプリを恒久的に変更する機能）はシステムの
ファイル関連付けをレジストリ越しに書き換える必要があり、影響範囲が大きいため
未実装（今後の拡張候補）。

---

## 35. Windows Explorer連携

- このアプリで開く
- Windows Explorerで開く
- Explorerから本アプリへフォルダを渡して開く
- コンテキストメニューから起動
- 連携ON/OFF

（実装状況）「Windows Explorerで開く」（右クリックメニュー。ファイルは親フォルダを
選択状態で開く）、起動引数でフォルダ／ファイルを渡された場合に最初のタブとして
開く機能（`ExplorerAlternative.exe "パス"`）、および設定画面のチェックボックスに
よるWindowsフォルダ右クリックメニューへの登録・解除（連携ON/OFF、
`HKEY_CURRENT_USER\Software\Classes\Directory(\Background)\shell` および
`Drive\shell` のみを使用するため管理者権限は不要）まで実装済み。

---

## 36. ブックマーク

現在の場所をブックマーク。

- `Ctrl + D`
- 名前変更
- 並び替え
- 削除
- 左ペイン表示

可能なら現在パス＋選択状態も保存。

（実装状況）名前変更・並び替え・削除・左ペイン表示まで実装済み。選択状態の保存は
未実装（パスのみ）。

（実装状況・2026-09更新）ユーザー指示によりショートカットを変更。`Ctrl + D`は
「選択したファイル・フォルダを複製」（後述）に割り当てを変更したため、ブックマーク
追加はショートカットキーなし（ハンバーガーメニュー「お気に入りに追加」・コマンド
パレット・ファイル右クリックメニューから実行）となった。機能自体（名前変更・
並び替え・削除・左ペイン表示、フォルダのドラッグ&ドロップ追加）は削除していない。

---

## 37. スマートタブ

同一フォルダの重複タブを抑制。

設定：

- 常に新しいタブ
- 既存タブを再利用
- 自動判定

（実装状況）実装済み。設定画面から3つの動作を切り替えられる（「自動判定」は
現状「既存タブを再利用」と同じ挙動とし、将来の判定拡張の余地として区別している）。

---

## 38. 巨大ファイル検索

**設定画面から実行可能**。

```text
巨大ファイル検索

対象：
○ 現在のフォルダ
○ 指定フォルダ
○ ドライブ

最小サイズ：
[ 500 MB ▼ ]

[検索]
```

結果：

```text
サイズ       ファイル
────────────────────────
4.8 GB       video.mp4
2.1 GB       backup.zip
850 MB       dump.bin
```

- サイズ降順
- バックグラウンド処理
- キャンセル可能

（実装状況）実装済み。ハンバーガーメニュー「ディスク解析...」から開く
（対象フォルダはテキスト入力＋フォルダ参照。「ドライブ」選択は対象フォルダに
ドライブのルートを指定することで代替）。重複ファイル検索（58章）・空フォルダ
検索（59章）と同じダイアログ・走査基盤（`IFolderScanService`）を共有する。

---

## 39. タスクバー / ジャンプリスト

Windowsタスクバーのジャンプリストに対応。

表示候補：

- 最近使ったフォルダ
- お気に入り
- ワークスペース
- よく使う場所

ワークスペースを直接起動可能。

（実装状況）`System.Windows.Shell.JumpList`（`JumpTask.CustomCategory`）により、
「最近使った場所」「お気に入り」「ワークスペース」の3カテゴリをタスクバーの
ジャンプリストへ反映する（最近使った場所・お気に入りの変化のたびに再構築）。
ワークスペースの項目は`ExplorerAlternative.exe --workspace "名前"`引数で直接
起動できる。

（実装状況・2026-09更新）「よく使う場所」（利用頻度の追跡）を実装した。
フォルダへ実際に移動するたびにアクセス回数を記録し（`AppSettings.FrequentPlaces`
に永続化）、回数の多い順（同数の場合は名前順）に上位5件を表示する。「最近使った
場所」（直近順のMRU）とは独立した頻度ベースの一覧として、左ペインに「最近使った
場所」の直後のセクションとして追加し、ジャンプリストにも4番目のカテゴリとして
反映する。

---

## 40. システムトレイ

設定から常駐ON/OFF。

トレイメニュー：

```text
高機能Explorer

最近の場所
お気に入り
ワークスペース
SSH
ターミナル

Explorerを表示
設定
終了
```

（実装状況）実装済み（`System.Windows.Forms.NotifyIcon`を使用。
`UseWindowsForms`をプロジェクトに追加し、WPF側と型名が衝突する
`System.Windows.Forms`/`System.Drawing`の暗黙usingのみを明示的に除外している）。
設定画面の「システムトレイに常駐する」チェックボックスでON/OFFを切り替える。
ONの場合、ウィンドウを閉じるとトレイへ格納され（終了はトレイメニューの
「終了」から）、トレイメニューは仕様通り「最近の場所」「お気に入り」
「ワークスペース」（各サブメニュー、クリックで該当箇所へ移動/読み込み）、
「SSH接続の管理...」「ターミナル表示/非切替」、「Explorerを表示」「設定」
「終了」を備える。

---

## 41. グローバルホットキー

他アプリ使用中でも本アプリを呼び出せる。

設定画面から変更可能。

競合時は警告。

（実装状況）実装済み（Win32 `RegisterHotKey`/`UnregisterHotKey`）。設定画面で
有効/無効・修飾キー（Ctrl/Alt/Shift/Win）・キー本体（WPFの`Key`列挙体名を
直接入力）を指定できる。他アプリと競合し登録に失敗した場合はエラーダイアログを
表示し、有効状態を元に戻す。

---

## 42. カスタム「送る」

設定から任意のソフトを登録。

登録：

- 表示名
- EXEパス
- 引数テンプレート
- アイコン

選択ファイル/フォルダを引数として渡す。

---

## 43. ワークスペース

複数ウィンドウを含むワークスペースを保存/復元。

保存対象：

- 複数ウィンドウ
- ウィンドウサイズ
- ウィンドウ位置
- 最大化状態
- タブ
- 各タブのパス
- ペイン分割
- ペインサイズ
- 表示モード
- 展開状態
- ターミナル
- Terminal Sync
- SSH接続
- Quick Look固定状態
- UI設定

---

## 44. SSH

SSH接続先を登録・管理。

登録：

- 表示名
- Host
- Port
- User
- 認証方式
- 秘密鍵
- パスフレーズ

秘密情報を平文保存しない。

接続後：

- リモート階層表示
- 詳細表示
- プレビュー
- コピー
- ダウンロード
- アップロード
- 名前変更
- 作成
- 削除
- SSHターミナル

（実装状況）接続先の登録・管理（表示名/Host/Port/User/秘密鍵パス）・保存・
編集・削除、およびSSHターミナル（OpenSSHクライアントを統合ターミナルへ委譲）
まで実装済み。パスワードは任意でWindows Credential Manager（資格情報
マネージャー）へ保存でき、settings.json等への平文保存は行わない
（「秘密情報を平文保存しない」を満たす）。接続時、保存済みパスワードが
あれば、ターミナル出力に"password"を含む行（"passphrase"は除く）が
現れた時点で自動入力する（画面には表示しない）。パスワード未保存の場合や
鍵のパスフレーズは、接続時にOpenSSH自身の対話プロンプトに委ねる。

（実装状況・2026-09更新）SFTPベースのリモートファイル操作を、専用の
リモートブラウザウィンドウ（「SSH接続の管理」→「SFTPで開く...」）として
実装した。SSH.NET（Renci.SshNet）ライブラリを追加し、リモート階層表示・
アップロード・ダウンロード・新しいフォルダ作成・名前変更・削除に対応する。
認証は、パスフレーズなしの秘密鍵（`IdentityFilePath`）があれば鍵認証を、
なければWindows Credential Managerに保存済みのパスワードを使う（どちらも
使えない場合はエラーメッセージを表示する）。パスフレーズ付き鍵は、
パスフレーズをどこにも保存しない方針上、直接のSFTP接続には対応しない
（従来通りSSHターミナル経由でのみ利用可能）。メインペインのローカル
ドライブとの統合（同一ツリー上でのシームレスな操作）までは行わず、
独立したブラウザ画面とした。プレビュー機能との統合は未実装。

---

## 45. フォルダ同期

分割ペイン間で同期。

```text
C:\Project
      ⇅
D:\Backup
```

- 差分確認
- 片方向同期
- 双方向同期
- 実行前確認
- コピー/削除対象確認

既存ファイルを不用意に削除しない。

（実装状況・2026-09更新）専用の2ペイン比較ウィンドウ（「☰ メニュー」→
「フォルダ比較・同期...」）として実装した。分割ビュー中に開いた場合は、
左右のペインの現在フォルダを比較対象の初期値として自動的に設定する。
比較は相対パス基準の再帰走査で、サイズが異なれば「内容が異なる」、
サイズが同じ場合のみSHA-256ハッシュを比較して同一/差分を判定する
（同一ファイルは一覧に表示しない）。差分一覧はチェックボックスで選択でき、
「選択項目を左→右にコピー」「右→左にコピー」のどちらかを実行前確認
ダイアログ付きで実行できる（双方向同期は、この2つのボタンを対象を変えて
使うことで実現する）。既存ファイルを不用意に削除しない方針のため、
削除・ミラーリング（片側にしかないファイルの削除）機能は持たない。

## 46. コマンドパレット

`Ctrl + Shift + P`

主要操作を検索・実行。

例：

- Open Terminal
- Create Patch
- Apply Patch
- Compare Files
- New Window
- Toggle Hidden Files
- Refresh
- Settings
- Split View
- Close Pane
- Open Workspace
- 巨大ファイル検索
- 操作履歴
- ブックマーク

---

## 47. ショートカットチートシート

アプリ内にショートカット一覧を用意。

カテゴリ：

- Navigation
- Explorer
- View
- Tabs
- Split View
- Terminal
- Git
- SVN
- Preview
- Search
- File Operations
- Workspace
- Command Palette

ショートカット変更時は自動反映。

（実装状況・2026-09更新）ジャンルごとにグループ化して表示するよう改善した
（`ShortcutEntry.Category`、チートシート画面はグループヘッダー付きで表示）。
現在のカテゴリ：移動／ファイル操作／プレビュー／タブ・ウィンドウ／ターミナル／
検索・その他。

---

## 48. 基本ファイル操作

- 開く
- コピー
- 切り取り
- ペースト
- 名前変更
- 削除
- 新規フォルダ
- 新規ファイル
- ドラッグ＆ドロップ
- プロパティ
- パスコピー

Windows 11風右クリックメニュー。

（実装状況・2026-09更新）ユーザー指示により、選択したファイル・フォルダを同じ場所に
複製する「複製」機能を追加し、`Ctrl + D`に割り当てた（自動的に「名前 (2)」のような
一意な名前を採番してコピーする）。これに伴い、新規フォルダ作成のショートカットは
`Ctrl + N`から`Ctrl + Shift + N`へ変更し、`Ctrl + N`は新しいウィンドウを開く操作
（現在のフォルダを引き継いで別プロセスを起動）に割り当てた。

（実装状況・2026-09更新）ダークテーマ時に右クリックメニュー・「☰ メニュー」の
ドロップダウンが白背景のまま残る不具合を修正した。原因は、`ContextMenu`と
`MenuItem`のApplication.Resourcesスタイルが背景色のSetterのみで、既定の
ControlTemplate（ポップアップ部分の chrome）にDynamicResourceのブラシが反映されない
環境だったこと（TabControlと同種の問題）。`ContextMenu`・`MenuItem`・`Separator`に
それぞれControlTemplateを明示的に定義し、確実にテーマ色を反映させた。あわせて、
アイコンやチェックマークを使わない項目でも既定テンプレートが確保していた左側の
空きガター列（アイコン/チェック用の余白）を撤去し、チェック可能な項目（タグ付与
メニュー等）のみチェック時に小さなチェックマークを表示する方式にした。

---

## 49. 隠しファイル

表示/非表示ボタンを用意。

対象：

`.git`, `.gitignore`, `.env`, `.vscode` 等。

状態を保存。

（実装状況）実装済み。ツールバーの「隠しファイル」ボタンで切替（既定は非表示）。
WindowsのHidden/System属性に加え、`.`で始まる名前（`.git`等、Windows上では
Hidden属性を持たないことが多い）も隠しファイルとして扱う。状態は設定に永続化。

---

## 50. 最近使った場所

左ペイン。

```text
🕘 最近使った場所
C:\Project
D:\Work
C:\Users\User\Downloads
server01:/home/user/project
```

履歴削除可能。

（実装状況・2026-09更新）ユーザー指示により、表示（および保存）件数の上限を
5件とした（`NavigationPaneViewModel.MaxRecentPlaces`）。

---

## 51. ピン留めファイル（廃止）

左ペインに「📌 ファイル」を追加。

よく使う：

- `.sln`
- `.csproj`
- `.md`
- 設定ファイル
- 任意ファイル

を直接開ける。

（実装状況・2026-09更新）一度実装したが、ユーザー指示により機能ごと削除した
（`NavigationPaneViewModel.PinnedFiles`・`AppSettings.PinnedFiles`・
右クリックメニュー「ピン留めに追加」等を削除）。今後復活させる場合は4章の
お気に入りとは別の一覧として再設計が必要。

---

## 52. ファイルロック

削除・移動等に失敗した場合、可能なら使用中プロセスを表示。

```text
このファイルを使用している可能性のあるプロセス：

chrome.exe
PID: 1234
```

---

## 53. ファイルサイズ

詳細表示等でサイズを表示。

フォルダサイズも非同期計算。

---

## 54. プロジェクト認識

以下のようなプロジェクトファイルを検出：

- `.sln`
- `.csproj`
- `package.json`
- `pom.xml`
- `Cargo.toml`
- `go.mod`
- `CMakeLists.txt`

プロジェクトとして扱い、プロジェクトルートへ移動できる。

（実装状況）実装済み（`IProjectDetectionService`）。VersionControlServiceの
Git/SVN判定と同様、現在パスから親方向へ探索する。一覧の`.sln`に加え、
dotnet CLIの新しいソリューション形式である`.slnx`も対象に含めている
（本プロジェクト自身がこの形式のため）。

---

## 55. 最近開いたプロジェクト

プロジェクト単位で履歴を管理。

```text
最近のプロジェクト

📦 MyApp
📦 TestTool
📦 ExcelConverter
```

（実装状況）実装済み。フォルダ移動によりプロジェクトが（前回と異なるものへ）
検出されるたびに記録する。ナビゲーションペインの「最近のプロジェクト」に
表示（削除・クリア可）。

（実装状況・2026-09更新）ユーザー指示により、同じ名前のプロジェクトが複数の
パスに存在する場合は1件のみ表示するようにした。上位階層（パスの区切り数が
少ない方）を優先し、階層が同じかそれ以上に深い場合は既存の項目を「最近使った」
扱いで先頭へ移動するにとどめる（別パスとして重複追加はしない）。

---

## 56. プロジェクトルート

コマンドから：

- Gitルート
- ソリューションルート
- プロジェクトルート

へ移動できる。

（実装状況）実装済み。右クリックメニュー「ルートへ移動」およびコマンド
パレットから、それぞれ検出されている場合のみ実行可能。

---

## 57. プロジェクト単位ターミナル

プロジェクトを開いた場合、必要に応じてプロジェクトルートをカレントディレクトリとしてターミナルを起動。

Terminal Syncと連動。

（実装状況）実装済み。Sync ON時、アクティブペインでプロジェクトが検出されている
場合はプロジェクトルートを、そうでない場合は現在フォルダをターミナルの
カレントディレクトリとする。

---

## 58. 重複ファイル検索

ファイル名だけでなくハッシュ等を使って同一内容のファイルを検出。

- 同一ファイル一覧
- サイズ
- パス
- ハッシュ
- 削除候補表示

削除は必ず確認。

（実装状況）実装済み（ディスク解析ダイアログ「重複ファイル検索」モード）。
同一サイズのファイルをグループ化した上でSHA-256ハッシュを計算して同一内容を
判定する。各グループの先頭1件は既定でチェックを外し、残りを削除候補として
提示する。

---

## 59. 空フォルダ検索

指定範囲の空フォルダを検索。

削除前に確認。

（実装状況）実装済み（同ダイアログ「空フォルダ検索」モード）。直下にファイルが
なく、かつ全てのサブフォルダも再帰的に空である場合のみ「空フォルダ」と判定
する（空フォルダのみを含む親フォルダも対象に含まれる）。

---

## 60. クイックコピー / クイック移動

コピー先・移動先の履歴を表示。

```text
最近の場所
├─ C:\Project
├─ D:\Backup
└─ C:\Work
```

---

## 61. 一時作業フォルダ

一時的な作業場所を提供。

設定：

- アプリ終了時に削除
- 保持
- 確認して削除

---

## 62. ファイル操作安全性

大量操作・削除・上書き等は対象と内容を明示。

危険操作は確認ダイアログを出す。

（実装状況・2026-09更新）67章Phase 7「操作履歴・GUI Undo」を実装した。移動・
コピー・名前変更・複製・新規作成（フォルダ/ファイル）・ショートカット作成・
一括リネームを対象に、アプリ全体で1つの操作履歴（スタック、最大50件）を共有し、
`Ctrl+Z`または「☰ メニュー」→「元に戻す」（次にUndoされる操作の内容をラベルに
表示）で直近の操作を1件ずつ元に戻せる。実行主体は`IUndoService`で、各操作の
完了直後に巻き戻し処理（例：移動なら元の親フォルダへ戻す）を記録する。Undo後の
表示更新は、対象フォルダを監視している`FileSystemWatcher`ベースの自動更新
（20章・64章）に任せており、Undo自体が個別に画面を再構築するわけではない。

以下は明示的にUndo対象外とする。
- **削除（ごみ箱送り）**：Windowsのごみ箱から特定の項目だけを復元するには、
  ごみ箱の内部形式を扱うCOM相互運用（`IFileOperation`等）が必要で、本実装の
  対象外とした。ごみ箱から手動で復元するか、Windowsエクスプローラー自体の
  Undoを利用する。
- **競合時の「上書きする」**（Ctrl+ドラッグで同名の項目が既に存在する場合）：
  上書きされた元の内容をバックアップしていないため、元に戻すと内容が失われる。
  「名前を変更してコピー」を選べばUndo対象になる。

Redo（やり直し）は対象外（今後の拡張候補）。

---

## 63. 設定

### 外観

- Light / Dark / System
- アクティブペイン背景強調度

### Explorer

- デフォルト表示モード
- 隠しファイル
- 拡張子
- アイコンサイズ
- 詳細表示列
- 左ペイン表示

### Terminal

- デフォルトシェル
- フォント
- フォントサイズ
- Syncデフォルト
- ターミナルテーマ

### Preview

- Preview有効/無効
- テキスト拡張子
- Markdown表示方式
- 最大プレビューサイズ

### Git / SVN

- executable path
- 状態表示

### SSH

- 接続先
- 認証情報

### Custom Send To

- 登録ソフト
- 引数

### 巨大ファイル検索

- デフォルト最小サイズ
- 検索対象
- 結果表示順

### Keyboard

- ショートカット変更
- チートシート

### Windows連携

- Explorerコンテキストメニュー
- ファイル関連付け
- システムトレイ
- ジャンプリスト
- グローバルホットキー

（実装状況・2026-09更新）設定画面はジャンルごとにタブ分けして見やすくした
（外観／タブ・ターミナル／Windows連携／拡張子／外部ツール）。本章に列挙された
設定項目のうち、実際に実装済みのものはこの5タブのいずれかに含まれる
（未実装の項目は各章の実装状況注記を参照）。

---

## 64. パフォーマンス

大量ファイルでもUIを固めない。

必須：

- 非同期I/O
- Lazy Loading
- UI Virtualization
- FileSystemWatcher
- バックグラウンド処理
- キャンセル可能検索
- 大容量ファイルの安全なプレビュー
- ハッシュ計算のバックグラウンド処理

UIスレッドで重い処理を実行しない。

（実装状況）検索・巨大ファイル検索・重複ファイル検索（ハッシュ計算含む）・
空フォルダ検索はバックグラウンド処理＋キャンセル可能（`IFolderScanService`、
`CancellationToken`）。テキストプレビューは`StreamReader.ReadBlock`で
先頭の必要な文字数のみを読み込み、ファイル全体をメモリに載せない。
`FileSystemWatcher`（`IFolderWatcherService`）により、他アプリ・ターミナル・
Git操作等による外部からのファイル変更を検知し、短時間の連続変化は
デバウンス（400ms）してから自動的に一覧を更新する（タブ/ペインを閉じる際に
監視も解放する）。UI Virtualizationは標準のListBox/ListViewの既定動作に
依拠している。

（実装状況・2026-09更新）同じフォルダの再読み込み（F5・上記の外部変更検知）に
限り、再読み込み前の選択状態・展開状態をフルパスで記録しておき、再構築後に
同じパスのノードへ復元するようにした（別フォルダへの移動時は対象外）。

不具合修正：この復元処理は当初、展開済みフォルダ1件ごとに表示リスト全体を
Clear→再構築してしまっており、展開済みフォルダの数が多い場合（例：30個の
フォルダを展開した状態でF5）に再構築が連鎖してアプリがフリーズしたように
見える不具合があった。展開状態の復元中は表示リストの再構築を抑制し、復元
完了後に1回だけ再構築するよう修正した。

---

## 65. セキュリティ

- SSH秘密情報を平文保存しない
- 外部コマンド引数を適切に扱う
- Git / SVN / PowerShell実行の安全性を考慮
- 不要な管理者権限を要求しない
- 危険操作には確認
- リモート操作も誤操作防止
- 任意EXE起動時の引数を適切にエスケープ

（実装状況）SSHパスワードはWindows Credential Manager経由で保存（44章）。
外部ツール実行（22章）は、対象パスに空白が含まれ、かつ引数テンプレート側で
未引用の場合に自動的に二重引用符で囲むよう修正済み（修正前は空白を含む
パスを渡すと引数が分割されてしまう不具合があった）。レジストリ操作
（Explorer連携・システムトレイ関連）・グローバルホットキー登録は
いずれもHKEY_CURRENT_USER配下／管理者権限不要なAPIのみを使用している。
削除は既定でごみ箱経由、破棄的なGit操作（Discard/Reset等）は確認ダイアログを
経由する。

---

## 66. 推奨アーキテクチャ

```text
Application
├─ UI
│  ├─ MainWindow
│  ├─ Explorer
│  ├─ Navigation
│  ├─ SplitView
│  ├─ Tabs
│  ├─ Terminal
│  ├─ Preview
│  ├─ Git
│  ├─ SVN
│  ├─ SSH
│  ├─ Search
│  ├─ Workspace
│  ├─ CommandPalette
│  └─ Settings
├─ Core
│  ├─ FileSystem
│  ├─ Navigation
│  ├─ Search
│  ├─ Preview
│  ├─ History
│  ├─ Tags
│  ├─ Bookmarks
│  ├─ Projects
│  └─ FileOperations
├─ VersionControl
│  ├─ Git
│  └─ SVN
├─ Terminal
│  └─ ConPTY
├─ Remote
│  └─ SSH
├─ Workspace
├─ Configuration
└─ Infrastructure
```

---

## 67. 実装フェーズ

### Phase 1：Explorer基盤

- WPFプロジェクト
- Windows 11風UI
- 左ペイン
- お気に入り
- タグ
- 階層表示
- 詳細表示
- 基本ファイル操作
- キーボード操作
- アドレスバー
- 履歴

（実装状況）完了。

### Phase 2：タブ・分割

- タブ
- スマートタブ
- Ctrlドラッグによるタブ複製
- 縦/横分割
- アクティブペイン
- 背景強調
- ペイン操作
- ペイン状態保存

（実装状況）完了。

### Phase 3：プレビュー

- Quick Look
- フォルダプレビュー
- Markdown
- テキスト
- 画像
- PDF
- Office
- 拡張子設定
- Quick Look固定
- コードシンボル

（実装状況）PDF・Office・コードシンボル表示を除き完了（外部ライブラリ追加が
必要な範囲のため見送り）。

### Phase 4：ターミナル

- PowerShell
- ConPTY
- 複数ターミナル
- Syncトグル
- `Ctrl + @`
- ドラッグ＆ドロップ
- ここでターミナル

（実装状況）ConPTY（ANSIカラー・シグナル伝達・リサイズ）を除き完了（17章参照）。

### Phase 5：Version Control

- Git
- SVN
- 自動判定
- Gitブランチ
- Diff
- ファイル比較
- Patch
- Source Control
- 状態表示

（実装状況）リモートブランチ表示・Log・Show Commitを除き完了（21章・25章参照）。

### Phase 6：SSH

- SSH登録
- 接続
- リモートExplorer
- ファイル転送
- SSHターミナル

### Phase 7：高度な操作

- 検索
- 一括リネーム
- 操作キュー
- 操作履歴
- GUI Undo
- 隠しファイル
- 最近使った場所
- ファイルロック
- ファイルサイズ
- 巨大ファイル検索
- 重複ファイル検索
- 空フォルダ検索
- フォルダ同期
- クイックコピー/移動

（実装状況）検索・隠しファイル・最近使った場所・ファイルサイズ・巨大ファイル
検索・重複ファイル検索・空フォルダ検索は実装済み。

（実装状況・2026-09更新）一括リネームは、パターン（連番・元の名前・拡張子・
日付）に加え、文字列置換・正規表現・大文字小文字変換・全角半角変換・
Unicode正規化まで実装済み（27章参照）。フォルダ同期も、専用の2ペイン比較
画面（差分確認・片方向/双方向コピー、実行前確認あり、削除は行わない）として
実装済み（45章参照）。

（実装状況・2026-09更新）「操作履歴・GUI Undo」を実装した（62章参照）。
操作キュー（大量コピー/移動の一時停止・再開・キャンセル）・ファイルロック
（使用中プロセス表示）・クイックコピー/移動は未実装（今後の拡張候補）。

### Phase 8：Windows連携・カスタマイズ

- アプリで開く
- Explorer連携
- ブックマーク
- ピン留めファイル
- タスクバー/ジャンプリスト
- システムトレイ
- グローバルホットキー
- カスタムSend To
- コマンドパレット
- ショートカットチートシート

（実装状況）完了（46章・その他各章参照）。

### Phase 9：プロジェクト・ワークスペース

- プロジェクト認識
- 最近のプロジェクト
- プロジェクトルート
- プロジェクト単位ターミナル
- ワークスペース保存/復元

（実装状況）完了（54章〜57章参照）。ワークスペース保存/復元はPhase 2の
段階で実装済み（17章・43章参照）。

### Phase 10：最終調整

- パフォーマンス
- エラー処理
- セキュリティ
- UI/UX改善
- 高DPI
- Releaseビルド
- インストーラー
- Windows 11との統合確認

（実装状況）パフォーマンス（FileSystemWatcherによる自動更新を追加。64章参照）・
セキュリティ（外部ツール引数の自動エスケープを追加。65章参照）に対応。
高DPIはWPF/.NETの既定のPerMonitorV2対応に依拠（追加のマニフェスト設定は
していない）。Releaseビルド・インストーラー作成・Windows 11との統合確認
（ジャンプリスト・トレイ・Explorer連携は35章・39章・40章で実装済みだが、
実機でのパッケージング確認は別途必要）は、コーディング作業というより
ビルド・配布・QA工程であるため本セッションでは対象外とし、今後の課題とする。

---

## 68. AIへの実装ルール

1. コンパイル可能なコードを生成する。
2. 未実装機能を黙って省略しない。
3. 実装できない機能は理由と代替案を明示する。
4. TODOだけで完成扱いにしない。
5. 既存コードの依存関係を確認する。
6. 巨大な1ファイルにすべてを入れない。
7. UIとロジックを分離する。
8. UIスレッドをブロックしない。
9. 非同期処理を適切に使用する。
10. エラー処理を入れる。
11. キーボード操作を重視する。
12. Windows 11のUI/UXを優先する。
13. 可能な限り実際にビルドしてエラーを修正する。
14. 機能追加時に既存機能を壊さない。
15. 外部ライブラリ追加時は採用理由とライセンスを確認する。
16. セキュリティ上危険な実装を避ける。
17. 長時間処理にはキャンセル手段を用意する。
18. 将来の機能追加を考慮した拡張可能な設計にする。
19. 仕様上の曖昧な部分は勝手に簡略化せず、合理的な実装案を提示する。
20. UI操作は可能な限りキーボードでも完結できるようにする。

---

## 69. 完成条件

- Windows 11で安定起動
- Windows 11風UI
- 階層表示
- 詳細表示
- お気に入り
- タグ
- ブックマーク
- ピン留めファイル
- メイン領域の縦/横分割
- アクティブペインの背景強調
- タブ
- スマートタブ
- Ctrlドラッグによるタブ複製
- 複数ウィンドウ
- ワークスペース保存/復元
- 高機能アドレスバー
- 検索
- Quick Look
- Markdownプレビュー
- フォルダプレビュー
- テキスト拡張子追加
- Officeプレビュー
- コードシンボル表示
- PowerShell
- `Ctrl + @`
- Terminal Sync
- Git / SVN自動判定
- Git / SVN状態表示
- Gitブランチ
- Diff
- Patch作成/適用
- SSH
- ファイル比較
- 比較対象保持
- 一括リネーム
- 操作キュー
- 操作履歴
- GUI Undo
- 操作前確認
- ドラッグ＆ドロップ強化
- 隠しファイル切替
- 最近使った場所
- 巨大ファイル検索
- 重複ファイル検索
- 空フォルダ検索
- ファイルロック確認
- カスタムSend To
- アプリで開く
- Windows Explorer連携
- タスクバー/ジャンプリスト
- システムトレイ
- グローバルホットキー
- コマンドパレット
- ショートカットチートシート
- プロジェクト認識
- 最近のプロジェクト
- プロジェクトルート
- プロジェクト単位ターミナル
- フォルダ同期
- 大量ファイルでも極端に重くならない
- Release EXEを生成できる
- インストーラーを生成できる

---

## 70. 開発開始時の指示

いきなり大量のコードを生成しない。

まず以下を提示する。

1. 全体アーキテクチャ
2. プロジェクト構成
3. NuGetパッケージ候補
4. 各機能の依存関係
5. 実装フェーズ
6. Phase 1で作成するファイル一覧
7. 技術的に難しい部分と実装方針
8. 各機能のテスト方針

その後Phase 1から実装する。

各Phase完了時には、

- ビルド可能
- 起動可能
- 既存機能が動作
- 次Phaseへ拡張可能

な状態を維持する。

仕様上の矛盾や技術的問題を発見した場合、勝手に仕様変更せず、問題点と推奨案を明示する。
