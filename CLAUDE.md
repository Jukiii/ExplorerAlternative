# Windows Explorer代替WPFアプリ
## Phase 1 実装仕様書

---

# 0. 正式仕様書との関係

本プロジェクトの正式・最新の仕様書は `docs/仕様.md`（v3、70章構成）である。

本ドキュメント（CLAUDE.md）は、開発初期に作成した縮小版（Phase 1向け）の仕様書であり、`docs/仕様.md` の内容の一部を抜粋・簡略化したものに過ぎない。両者の内容が矛盾する場合は、常に `docs/仕様.md` を優先する。

実装がPhase 1の範囲を超えて進んでいる場合（例：Git/SVNの実操作、Patch、SSH接続など）、本ドキュメントの該当章は既に古くなっている可能性がある。作業前に必ず `docs/仕様.md` の該当章と現在の実装状況（git log、既存コード）を確認すること。

---

# 1. この仕様書の目的

本仕様書に従って、Windows Explorerの代替となるデスクトップアプリケーションを実装する。

本プロジェクトでは、Windows Explorerのファイル操作機能に加えて、Visual Studio Codeのようなターミナル統合、MacのQuick Lookに近いプレビュー機能、Git/SVN情報表示などを統合する。

Phase 1では、将来的な拡張を考慮した構造を作りつつ、仕様書に定義された機能のみを実装する。Phase 1完了後の拡張フェーズについては `docs/仕様.md` 67章「実装フェーズ」の区分（Phase 2〜10）に従う。

---

# 2. 技術要件

## 2.1 必須技術

- C#
- WPF
- .NET 10（`docs/仕様.md` 1章に準拠。既存プロジェクトも `net10.0-windows` を使用）
- Windows

## 2.2 ターミナル

- PowerShellを使用する。
- Explorerとターミナルの現在フォルダを連携できる構造にする。

## 2.3 UI

Windows標準アプリとして自然に使用できるUIを採用する。

基本的に日本語UIとする。

---

# 3. 基本画面構成

画面を以下の領域に分ける。

```text
┌─────────────────────────────────────────────┐
│ タブ                                      ＋ │
├─────────────────────────────────────────────┤
│ ナビゲーション / ツールバー                  │
├──────────────┬──────────────────────────────┤
│              │                              │
│ ナビゲーション │       メインペイン           │
│              │                              │
│ お気に入り    │  フォルダ・ファイル階層       │
│ タグ          │                              │
│              │                              │
├──────────────┴──────────────────────────────┤
│ PowerShell Terminal                          │
│                                              │
└─────────────────────────────────────────────┘
```

ただし、画面構成は将来的な分割ペインに対応できる設計にする。

---

# 4. メインペイン

## 4.1 ファイル・フォルダ表示

メインペインには、現在のフォルダ以下のフォルダとファイルを表示する。

通常のExplorerのような「フォルダ一覧」と「ファイル一覧」を別々に表示する方式ではなく、階層構造を持った表示を基本とする。

例：

```text
> Documents
  > Project
    > src
      > Main
        main.cs
        config.json
      README.md
  > Test
    test.txt
```

## 4.2 階層表示

フォルダの展開状態を以下の記号で表示する。

- `>` : 折りたたまれている
- `v` : 展開されている

`+` / `-` は使用しない。

フォルダとファイルを同じ階層構造内に表示する。

---

# 5. 表示モード

メインペインには以下の表示モードを用意する。

- 詳細表示
- 階層表示（主要モード）

`docs/仕様.md` 6章の通り、単純な「リスト表示」（通常表示）は用意しない。現在の実装（`ViewMode` enum: `Detail` / `Tree`）もこれに準拠している。

表示モードはユーザーが切り替えられるようにする。

---

# 6. ナビゲーションペイン

左側にナビゲーションペインを配置する。

## 6.1 お気に入り

ユーザーが登録したフォルダをお気に入りとして表示する。

## 6.2 タグ

ファイル・フォルダに設定したタグを一覧表示できるようにする。

## 6.3 折りたたみ

ナビゲーションペインは折りたたみ可能とする。

メインペインを広く使えるようにする。

---

# 7. アドレスバー

Windows 11のExplorerに近いパンくず形式のアドレスバーを実装する。

例：

```text
PC > C: > Users > User > Documents > Project
```

## 7.1 パンくず操作

各階層をクリックして、そのフォルダへ移動できる。

## 7.2 ドロップダウン

各フォルダの階層について、ドロップダウンからフォルダを選択できるようにする。

## 7.3 パスの部分置換

アドレスバー上の一部のパスを選択して変更できるようにする。

---

# 8. フォルダ移動

## 8.1 親フォルダ

以下のショートカットを実装する。

```text
Alt + ↑
```

現在のフォルダの親フォルダへ移動する。

---

# 9. PowerShellターミナル

画面下部にPowerShellターミナルを配置する。

Visual Studio Codeの統合ターミナルに近い操作感を目指す。

## 9.1 表示・非表示

以下のショートカットでターミナルを切り替える。

```text
Ctrl + @
```

注意：

```text
Ctrl + '
```

ではない。

必ず `Ctrl + @` とする。

## 9.2 同期機能

ターミナル上部左側に同期ON/OFF切り替えUIを配置する。

```text
[同期 ON/OFF]
```

### 同期ON

Explorer側で現在フォルダを変更した場合、ターミナルのCurrent Directoryも同じフォルダへ変更する。

### 同期OFF

Explorer側でフォルダを変更しても、ターミナルのCurrent Directoryを変更しない。

（実装状況）Phase 1完了後、複数ターミナルタブに対応済み。タブごとに独立した
Sync ON/OFF状態を持つ。ターミナル表示時、およびターミナル部分（出力欄含む）を
クリックした際に入力欄へ自動フォーカスする。詳細は `docs/仕様.md` 17〜19章。

---

# 10. ターミナルとExplorerの連携

ターミナルとExplorerの連携方式には以下の考え方を採用する。

## 10.1 連動モード

Explorerとターミナルを連動させる。

## 10.2 独立モード

Explorerとターミナルを独立して操作できる。

将来的に、ターミナルを複数ペインで使用することも考慮する。

（実装状況）複数ターミナルタブとして実装済み（9.2章参照）。

---

# 11. プレビュー

MacのQuick Lookに近いプレビュー機能を実装する。

## 11.1 基本操作

ファイルまたはフォルダを選択した状態で、Spaceキーによってプレビューを表示できるようにする。

## 11.2 Markdown

MarkdownファイルはMarkdownとしてレンダリングした内容をプレビューする。

単純なテキスト表示ではなく、Markdownの見た目を確認できる形式とする。

## 11.3 フォルダ

フォルダを選択した場合もプレビュー対象とする。

---

# 12. Git / SVN情報

現在のフォルダまたはその祖先フォルダにGit / SVNの管理情報が存在する場合、その情報を表示する
（`docs/仕様.md` 20章「現在パスから親方向へ探索」に準拠。本章の旧文言は「下位階層」としていたが、
下位階層は探索しないよう修正済み）。

## 12.1 Git

`.git` が現在のフォルダまたはその祖先フォルダに存在する場合、Git情報を表示する。

## 12.2 SVN

SVN管理情報が現在のフォルダまたはその祖先フォルダに存在する場合、SVN情報を表示する。

## 12.3 非管理フォルダ

Git / SVNのどちらにも該当しない場合、Git/SVN情報を表示しない。

（実装状況）表示は上部バナーではなく、表示/非表示を切り替え可能な右側ペインで
行う（既定は表示）。ファイル・フォルダ一覧にはM/A/D/U/Rのステータスバッジも
表示する。詳細は `docs/仕様.md` 21章。

---

# 13. Git / SVN操作

将来的なバージョン管理操作を考慮したUI構造にする。

Phase 1では、少なくともGit/SVNの状態を判定して表示できる構造を作る。

（実装状況）Phase 1完了後、ステージ・コミット・プッシュ・プル・Fetch・ブランチ
切替・新規ブランチ作成・Merge・Rebase・Stash・Stash Pop・Clone・Initialize・
Discard Changes（Git）、更新（SVN）まで実装済み。実行は資格情報プロンプト等で
UIをブロックしないよう、統合ターミナル（9章）へコマンドを委譲する方式を
採用している。Log/Show Commit（`docs/仕様.md` 21章）は未実装であり、今後の
拡張候補とする。

---

# 14. Patch機能

Patchの作成・適用に対応する。

## 14.1 Patch作成

現在の変更内容からPatchを作成できるようにする。

## 14.2 Patch適用

Patchファイルを選択して適用できるようにする。

Git/SVNなどのバージョン管理システムとの連携を考慮した設計にする。

（実装状況）Patch作成・適用は実装済み（`git diff`/`svn diff`、`git apply`/`svn patch`）。Patch内容確認・PatchからのDiff表示（`docs/仕様.md` 23章・24章）は未実装。

---

# 15. SSH

SSH接続機能を追加できる構造にする。

将来的にリモート環境のファイル操作やターミナル操作に拡張できるようにする。

（実装状況）接続先の登録・管理（表示名・Host・Port・User・秘密鍵パス）と
永続保存、`ssh` コマンドを組み立てて統合ターミナルへ送信して接続する方式まで
実装済み。パスワードは任意でWindows Credential Managerへ安全に保存でき、
接続時に自動入力される（settings.jsonには含めない）。リモート階層表示・
ファイル転送等（`docs/仕様.md` 44章）は未実装。

---

# 16. テキストとして扱う拡張子

通常はテキストファイルとして認識されない拡張子についても、ユーザーが設定によってテキストファイルとして扱えるようにする。

例：

```text
.xxx
.yyy
.custom
```

設定画面から拡張子を追加できる構造にする。

---

# 17. ワークスペース

複数の作業環境を保存できるようにする。

ワークスペースには少なくとも以下を保存できる構造にする。

- ウィンドウサイズ
- ウィンドウ位置
- 開いているタブ
- 各タブの状態
- 分割ペインの状態
- 各ペインの表示状態

---

# 18. タブ

複数のフォルダをタブとして開けるようにする。

## 18.1 タブ複製

以下の操作に対応する。

```text
Ctrl + ドラッグ
```

タブをCtrlキーを押しながらドラッグすると、そのタブを複製する。

---

# 19. 分割ペイン

メイン領域を複数のペインに分割できるようにする。

## 19.1 分割対象

分割できるのはメインペインのみとする。

## 19.2 アクティブペイン

現在操作対象となっているペインを明確に表示する。

## 19.3 ツール操作

以下の操作対象は、現在アクティブになっているペインとする。

- フォルダ移動
- ファイル操作
- ターミナル連携
- 表示モード変更
- その他のメインペイン操作

---

# 20. コンテキストメニュー

ファイル・フォルダの右クリックメニューを実装する。

少なくとも将来的に以下を追加できる構造にする。

- 開く
- コピー
- 移動
- 名前変更
- 削除
- Patch
- Git/SVN関連操作
- 外部ツール

---

# 21. 一括名前変更

複数のファイルを選択し、一括で名前を変更できるようにする。

コンテキストメニューから起動できるようにする。

---

# 22. 外部ツール

ユーザーが任意の外部ツールを追加できるようにする。

設定画面から、

- ツール名
- 実行ファイル
- 引数
- 対象

などを設定できる構造にする。

---

# 23. チートシート

アプリ内で利用できるショートカットを確認できるチートシートを用意する。

少なくとも以下を掲載する。

| 操作 | ショートカット |
|---|---|
| ターミナル表示/非表示 | Ctrl + @ |
| 親フォルダへ移動 | Alt + ↑ |
| プレビュー | Space |

今後追加されるショートカットもここへ追加できる構造にする。

---

# 24. 日本語UI

UIは日本語を基本とする。

ボタン、メニュー、設定画面、エラーメッセージなど、ユーザーが直接操作する部分は日本語で表示する。

ただし、以下のような技術名称は原文のままでよい。

- Git
- SVN
- PowerShell
- SSH
- Markdown
- Windows
- WPF

---

# 25. 設定

アプリケーション設定を一元管理できる構造にする。

将来的に以下を設定可能にする。

- テキストとして扱う拡張子
- 外部ツール
- お気に入り
- タグ
- ターミナル設定
- 表示設定
- ショートカット
- ワークスペース

---

# 26. アーキテクチャ

UIとロジックを可能な限り分離する。

以下のような責務分離を基本とする。

```text
UI
 ↓
ViewModel
 ↓
Service
 ↓
OS / File System / PowerShell
```

ファイル操作やPowerShell操作などをViewに直接記述しない。

将来的な機能追加・テストが容易な構造にする。

---

# 27. エラーハンドリング

以下の場合にアプリがクラッシュしないようにする。

- 存在しないファイルへのアクセス
- アクセス権限がないフォルダ
- ファイル削除失敗
- ファイル移動失敗
- PowerShell起動失敗
- Git/SVN情報取得失敗
- SSH接続失敗
- 不正な設定値

ユーザーには日本語で分かりやすいエラーを表示する。

---

# 28. Phase 1での実装方針

Phase 1では、すべての高度な機能を完全実装することよりも、

**今後の機能追加に耐えられるアプリケーション基盤を作ること**

を重視する。

特に以下の構造を優先する。

1. WPFアプリケーションの基本構造
2. メイン画面
3. ナビゲーションペイン
4. メインペイン
5. ファイル・フォルダ階層表示
6. 表示モード切り替え
7. パンくずアドレスバー
8. 基本的なフォルダ移動
9. PowerShellターミナル
10. Explorerとターミナルの同期
11. Spaceによるプレビュー
12. 基本的な設定管理
13. 将来のGit/SVN・SSH・Patch・外部ツールへの拡張基盤

---

# 29. 実装上の禁止事項

以下は禁止する。

- 仕様にない機能を勝手に追加する
- 既存機能を理由なく削除する
- UIだけ作って実際の動作を実装しない
- ダミーデータだけで実装を完了したことにする
- エラーを握りつぶす
- ハードコードによって将来の拡張を困難にする
- Viewに大量のビジネスロジックを書く
- ユーザーが指定したショートカットを変更する

特に、

```text
Ctrl + @
Alt + ↑
Space
```

のショートカットは仕様通りに実装する。

---

# 30. Claudeへの実装手順

以下の順番で作業すること。

## Step 1

現在のプロジェクト構成を確認する。

以下を確認する。

- .sln
- .csproj
- ソースコード
- XAML
- NuGetパッケージ
- 設定ファイル

既存コードが存在する場合、まず内容を理解する。

## Step 2

現在の実装と本仕様書を比較する。

以下を報告する。

```text
既に実装済み：
未実装：
仕様と異なる部分：
問題点：
```

## Step 3

実装計画を作成する。

機能を適切な単位に分割する。

## Step 4

実装する。

一度にプロジェクト全体を大幅に書き換えない。

機能単位で実装する。

## Step 5

ビルドする。

ビルドエラーがあれば修正する。

## Step 6

実装した機能を確認する。

少なくとも以下を確認する。

- アプリが起動する
- フォルダを移動できる
- ファイル・フォルダが表示される
- 表示モードが切り替わる
- Alt + ↑ が動作する
- Spaceでプレビューできる
- Ctrl + @ でターミナルを切り替えられる
- ターミナルが起動する
- 同期ON/OFFが動作する

## Step 7

最後に変更内容を報告する。

以下の形式で報告する。

```text
## 実装結果

### 変更したファイル
- xxx.cs
- xxx.xaml
- xxx.csproj

### 実装内容
- xxx
- xxx
- xxx

### ビルド結果
成功 / 失敗

### 動作確認
- [x] xxx
- [x] xxx
- [ ] xxx

### 残課題
- xxx
```

---

# 31. 重要な判断ルール

仕様が曖昧な場合、Claudeが独自判断で仕様を変更してはいけない。

以下の優先順位で判断する。

```text
1. docs/仕様.md（正式・最新の仕様書）に明記されている内容
2. この仕様書（CLAUDE.md）に明記されている内容
3. プロジェクト内の既存仕様・設計
4. 一般的なWPF/C#の設計原則
5. Claudeの推測
```

`docs/仕様.md` と本ドキュメントが矛盾している場合は `docs/仕様.md` を優先する（0章参照）。仕様書と既存コードが矛盾している場合は、勝手に変更せず、その矛盾を報告する。

---

# 32. 実装開始指示

この仕様書を読み終えたら、まずプロジェクト全体を確認すること。

**いきなりコードを書き始めないこと。**

最初に、

1. 現在のプロジェクト構成
2. 現在実装されている機能
3. 不足している機能
4. 仕様との矛盾
5. 実装計画

を整理して報告する。

その後、実装を開始する。

仕様にない機能を追加する必要がある場合は、実装前に確認すること。
